version: "3.9"

services:

  web:
    build:
      dockerfile: Dockerfile
      context: web/
    #container_name: web

    ports:
      - 8080:80
    volumes:
      - /home/luciann/training/php_mariadb_app/web:/var/www/html

  db:
    build:
      dockerfile: Dockerfile
      context: db/
   # container_name: db

    volumes:
      - /home/luciann/training/php_mariadb_app/db:/var/lib/mysql

    environment:
      MYSQL_ROOT_PASSWORD: pw123
      MYSQL_DATABASE: wordpress
